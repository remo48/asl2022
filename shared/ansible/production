[dbservers]
db
[webservers]
web

[configservers]
config  ansible_connection=local

[clientservers]
client

[backupservers]
backup

[caservers]
ca

[backupclients]
config  ansible_connection=local
ca
db

